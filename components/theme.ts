import type { DefaultTheme } from "styled-components";

const theme: DefaultTheme = {
	colors: {
		primary: "#3b3028",
		primaryLight: "#6F3F18",
		secondary: "#e9e3d9",
	},
};

export default theme;
